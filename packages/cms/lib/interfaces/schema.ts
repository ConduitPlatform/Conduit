const schema = {
    name: 'SchemaDefinitions',
    modelSchema: {
        name: {
            type: String,
        },
        //todo The properties in JSON, replace adequetly
        modelSchema: {
            type: String
        },
        //todo The properties in JSON, replace adequetly
        modelOptions: String
    },
    modelOptions: {
        timestamps: true
    }
};
export default schema;
