{
  "name": "conduit",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "private": true,
  "engines": {
    "node": ">=14"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "docz:dev": "docz dev",
    "docz:build": "docz build",
    "docz:serve": "docz build && docz serve",
    "build:bootstrap": "lerna run build",
    "build:docker": "docker build -t quintessential.azurecr.io/conduit .",
    "push:docker": "docker push quintessential.azurecr.io/conduit",
    "deploy:docker": "npm run build:docker && npm run push:docker"
  },
  "workspaces": [
    "packages/*",
    "libraries/*",
    "admin/*",
    "modules/*"
  ],
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "copyfiles": "^2.2.0",
    "lerna": "^3.13.1",
    "pm2": "^4.4.0"
  },
  "dependencies": {
    "docz": "^2.3.1",
    "prettier": "^2.2.1",
    "react": "16.12.0",
    "react-dom": "16.12.0"
  }
}
