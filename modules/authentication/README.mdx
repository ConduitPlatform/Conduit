# Authentication Module

This module offers user authentication.<br />
It supports logins with local credentials as well as popular identity providers.<br />
TwoFactor authentication is also provided.

## Features ✔️

- Local authentication with email/password
- Optional email verification on local strategy
- Forgot password / Reset password
- Phone authentication
- Facebook login with client credentials
- Google login with client credentials
- Twitch login with redirect strategy
- Figma
- GitHub
- Microsoft
- Slack
- Service account/ API key
- Optional 2FA supported on all strategies
- Optional Account linking supported when email is the same between providers

## Requirements ⚡

- [Database](../database) module
- [Email](../email) module (optional: local authentication + email verification)

## Environment Variables 📃 <a name="env-vars"></a>

|       Variable       | Description                                          | Required |      Example       |
|:--------------------:|:-----------------------------------------------------|:--------:|:------------------:|
|   `CONDUIT_SERVER`   | Conduit Core's address and port                      |   True   |  `0.0.0.0:55152`   |
|     `SERVICE_URL`    | This should be where this service listens on. If behind a LB it should point to the LB's IP/DNS  |   False   |  `0.0.0.0:55190`   |
|     `GRPC_PORT`      | The port number the gRPC server will listen to                      |  False   |  `55190`           |
|     `GRPC_KEY`       | Specifying a secret enables gRPC signed request protection (**use across modules**) |  False   | `someRandomSecret` |
