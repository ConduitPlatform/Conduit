export default {
  active: {
    format: 'Boolean',
    default: true,
  },
  // If set to false the forms module will utilize the storage module for sending blobs
  useAttachments: {
    format: 'Boolean',
    default: true,
  },
};
