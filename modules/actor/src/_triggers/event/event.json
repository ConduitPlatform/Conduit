{
  "name": "Event Trigger",
  "description": "Triggers a flow when a specified event occurs inside the Conduit cluster",
  "options": {
    "eventName": {
      "type": "string",
      "nullable": false
    }
  }
}
