on:
  push:
    branches:
      - master
    paths:
      - "modules/email/**"
      - "libraries/grpc-sdk/**"
jobs:
  deploy:
    runs-on: ubuntu-18.04
    name: Build email
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Docker Login
        uses: docker/login-action@v1.6.0
        with:
          registry: ${{ secrets.REGISTRY }}
          username: ${{ secrets.REGISTRY_USERNAME }}
          password: ${{ secrets.REGISTRY_PASSWORD }}
      - name: Build conduit email
        run: |
          DEV=TRUE make modules/email

  rollout-proxy:
    runs-on: ubuntu-latest
    name: Redeploy email
    needs: [deploy]
    steps:
      - uses: fjogeleit/http-request-action@master
        with:
          url: "http://20.101.9.248/email-provider/conduit-grp/"
          method: "POST"
          username: admin
          password: ${{ secrets.KUBECTLPROXY_PASSWORD }}
