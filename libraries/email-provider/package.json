{
  "name": "@conduit/email-provider",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "prepublish": "npm run build",
    "build": "tsc",
    "publish": "npm publish",
    "test": "cross-env TS_NODE_FILES=true mocha --timeout 10000 --reporter nyan -r ts-node/register test/**/*.ts",
    "coverage": "nyc r lcov -e .ts -x \"*.test.ts\" npm run test"
  },
  "license": "ISC",
  "dependencies": {
    "@types/nodemailer": "^6.4.0",
    "@types/nodemailer-mailgun-transport": "^1.4.1",
    "nodemailer": "^6.4.6",
    "nodemailer-mailgun-transport": "^2.0.0"
  },
  "directories": {
    "lib": "lib"
  },
  "files": [
    "lib"
  ],
  "publishConfig": {
    "registry": "http://registry.quintessential.gr/"
  },
  "devDependencies": {
    "@types/chai": "^4.2.11",
    "@types/mocha": "^7.0.2",
    "chai": "^4.2.0",
    "cross-env": "^5.2.0",
    "mocha": "^7.1.1",
    "nyc": "^15.0.0",
    "rimraf": "^3.0.2",
    "ts-node": "^8.8.1",
    "typescript": "^3.7.5"
  }
}
