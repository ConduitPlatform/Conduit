{
  "name": "@conduit/protos",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "rimraf ./dist/src && sh proto.sh",
    "postbuild": "copyfiles -u 1 src/**/*.proto src/**/*.json ./dist/src/proto",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/google-protobuf": "^3.2.7",
    "google-protobuf": "^3.8.0-rc.1"
  },
  "peerDependencies": {
    "grpc": "1.24.2"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "grpc": "1.24.2",
    "copyfiles": "^2.2.0",
    "grpc-tools": "^1.8.1",
    "ts-protoc-gen": "^0.12.0"
  }
}
